/** Three-day cache window — metrics are marketing stats, not real-time dashboards. */
export const PLATFORM_METRICS_REVALIDATE_SECONDS = 60 * 60 * 24 * 3;

export const PLATFORM_METRICS_CACHE_TAG = "platform-metrics";

export const PLATFORM_METRICS_SNAPSHOT_ID = "global";
