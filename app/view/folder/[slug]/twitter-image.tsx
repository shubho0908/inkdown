import ViewFolderOpenGraphImage, {
  alt,
  contentType,
  size,
} from '@/app/view/folder/[slug]/opengraph-image'

export { alt, contentType, size }

export const runtime = 'nodejs'

export default ViewFolderOpenGraphImage
